### 📦 Mensaje de commit sugerido

```bash
git commit -m "feat(agent): implementar contexto persistente y retroalimentación detallada

- Carga única de skills (core, test_design, fix_design, documentation) al iniciar el contenedor.
- Mantiene contexto (archivo vulnerable, análisis SAST) durante toda la vida del agente.
- En phase_test_design, el prompt base incluye skills + análisis + archivo.
- En reintentos, añade feedback completo: código del test, comando, salida, código retorno.
- Implementa logging persistente en archivo dentro del worktree para trazabilidad.
- El orquestador incluye ahora 'sast_analysis' detallado en input.json.

Esto soluciona el problema de desviación del modelo en test_designing y permite depuración exhaustiva."