# Core
docker>=6.1.0
aiofiles>=23.2.0
python-dotenv>=1.0.0

# SOTA Agent (dentro del contenedor ya está, pero para el host/VM)
requests>=2.31.0
pyyaml>=6.0.1

# GitHub API
PyGithub>=2.1.0

# SAST (opcional, algunas herramientas ya están en el contenedor)
semgrep>=1.44.0
bandit>=1.7.5